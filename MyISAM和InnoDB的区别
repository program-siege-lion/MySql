MyISAM的优点：
  
Innodb的优点：
  遵循ACID模型，提供事务，并具有提交 回滚和崩溃恢复功能，以保护用户数据。
  行级锁定和Oracle风格的一直读取，可提高多用户并发性和性能。
  每个InnoDB都有一个成为聚集索引的主键索引，该索引组织数据以最小化主键查找的I/O。
  提供外键支持，维护数据完整性，InnoDB支持FOREIGN KEY约束，使用外键检查插入，更新和删除操作，以确保他们不会导致不同表之间不一致。
  支持B树索引，备份时间点恢复，聚簇索引，压缩数据，资料快取，加密数据，外键支持，地理空间数据类型支持，地理空间索引支持，MVCC, 索引缓存，主从复制，全文索引，存储限制64T等等
  
Innodb和MyISAM的区别：
